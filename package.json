{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "./client",
    "./server",
    "./shared"
  ],
  "scripts": {
    "dev": "bun --filter client --filter server dev",
    "build": "bun --filter client --filter server build",
    "start": "bun --filter client --filter server start",
    "fmt": "oxfmt --write",
    "lint": "oxlint --fix"
  },
  "devDependencies": {
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0",
    "typescript": "^5.9.3"
  }
}
